const { expect } = require('chai');

describe('Testing user handler', function () {
    
    
    it('Call Users List and make sure your user id doesn’t exist.', function (done) {
        
    });
    
    it('Create User', function (done) {
        
    });
    
    it('Call User List and verify created user is part of user list.', function (done) {
        
    });

    it('Delete User', function (done) {
        
    });
    
    it('Call User List and verify deleted user is no longer part of user list.', function (done) {
        
    });   
});
